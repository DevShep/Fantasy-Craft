'use strict';

/**
 * @ngdoc service
 * @name fcApp.CharManager
 * @description
 * # CharManager
 * Service in the fcApp.
 */
angular.module('fcApp')
  .service('CharManager', function CharManager() {
    // AngularJS will instantiate a singleton by calling "new" on this function
  });
